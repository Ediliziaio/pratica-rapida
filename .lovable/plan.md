

## Piano: Rinominare la rotta `/home` in `/offerta`

### Modifiche

1. **`src/App.tsx`** — Cambiare `path="/home"` in `path="/offerta"`
2. **`src/components/landing/Navbar.tsx`** — Aggiornare eventuali link interni che puntano a `/home`
3. **`src/components/landing/Footer.tsx`** — Aggiornare eventuali link interni che puntano a `/home`
4. **Qualsiasi altro file** che referenzia `/home` come rotta — verificare con search

